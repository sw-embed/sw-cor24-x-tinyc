Implement adjacent string-literal token concatenation per C99 translation phase 6. Today the expression parser concatenates adjacent StringLits in expression position (expr.rs:157-168) and the global initializer path inspects the parsed expression for size inference (decl.rs:312-318), but the local declaration path (stmt.rs:254-256) peeks ONLY the first StringLit before parse_assign, so 'char s[] = "abc" "def";' gets sizeof=4 instead of 7. Mirror the decl.rs pattern in stmt.rs: parse_assign first, then inspect the resulting Expr::StringLit and set the inferred array size from the concatenated string. Verify the brace-init path for 'char *strings[] = {"a" "b", "c" "d"}' also routes through expr concat. Add parser tests for: pointer init, return expression, function argument, char[] sizeof correctness with adjacent literals, multi-line, comments between literals, per-chunk escape resolution. Add demo63 with e2e coverage. Update README. Brief: /disk1/github/softwarewrighter/devgroup/tools/briefs/dcxtc-string-literal-concatenation.md