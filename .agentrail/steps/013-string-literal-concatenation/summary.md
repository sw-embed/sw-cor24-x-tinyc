Found and fixed real bug: stmt.rs:254 peeked first StringLit before parse_assign for char[] size inference, so 'char s[] = "abc" "def";' inferred sizeof=4 instead of 7. Mirrored the global path (decl.rs) by parsing first, then inspecting Expr::StringLit. Added 12 parser tests, demo63 with e2e coverage (escape per chunk, multi-chunk, function return/arg, sizeof correctness), reg-rs lock-in, README. Discovered and surfaced two pre-existing orthogonal codegen issues (char* global init, char[] local indexing) and avoided them in the demo. Same source-vs-binary pattern as saga A: May-3 PATH binary lacks all of this; once mike rebuilds and reinstalls, dcpls's PL/SW saga unblocks.