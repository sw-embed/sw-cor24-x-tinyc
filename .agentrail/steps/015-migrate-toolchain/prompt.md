Apply dc-migrate-toolchain.md to sw-cor24-x-tinyc. Pre-flight grep showed 64 demo runners + run-subset-tests.sh + run-chibicc-test.sh use 'cor24-run --run x.s [opts]'. Replace each with 'cor24-asm x.s -o /tmp/x.lgo && cor24-emu --lgo /tmp/x.lgo [opts]' per the canonical mapping. cor24-emu (May 8) has no internal assembler (per dcemu's pr/remove-internal-assembler shipped); cor24-run (April 17) is slated for retirement. This unblocks pr/emit-zero-fill (which needs cor24-emu's path because cor24-run's internal assembler mishandles .zero N). Robustness folds-in: mkdir -p build where writes occur, replace any $HOME/Mac-only paths. Verify byte-equivalent: every demo must produce the same r0/halt/UART output as before. Brief: /disk1/github/softwarewrighter/devgroup/tools/briefs/dc-migrate-toolchain.md