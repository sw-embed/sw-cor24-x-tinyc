Support sizeof on expressions without parentheses: 'sizeof x' in addition to 'sizeof(type)'. Parser: if token after sizeof is not '(', parse as unary expression and infer type. If '(' followed by type keyword, parse as sizeof(type). If '(' followed by expression, treat parens as grouping. Use expr_type() from tc24r_type_infer to determine the expression's type and emit its size. Verify chibicc variable.c passes. Refs: #9