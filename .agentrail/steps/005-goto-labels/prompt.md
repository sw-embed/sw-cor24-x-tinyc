Support goto statements and labels. Parser: recognize 'goto ident;' as Stmt::Goto(name) and 'ident:' as Stmt::Label(name) — distinguish labels from expressions by checking for colon after identifier. AST: add Stmt::Goto(String) and Stmt::Label(String). Codegen: emit label as assembly label, goto as 'bra' (unconditional branch). Verify chibicc control.c passes (requires empty-for from previous step). Refs: #3