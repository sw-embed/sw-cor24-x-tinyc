Added octal integer literal support to lexer (read_octal method, 0-prefix detection). Removed octal-stripping line from chibicc test awk filter. Fixed binary literal filter to match uppercase 0B. Verified octal values correct (0777=511, 010=8, etc). All 13 chibicc tests still pass, no regressions. literal.c test still blocked by other features (wide chars, scientific notation) addressed by later steps.