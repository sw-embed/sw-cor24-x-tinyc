Fixed two symmetric codegen bugs surfaced during pr/string-literal-concatenation. Bug 1: char *g="abc" global emitted bytes at _g instead of a pointer to anon rodata; fixed in emit_data_section by routing Type::Ptr+StringLit through state.string_literals + emit '.word _Sn'. Bug 2: char a[]="abc" local stored rodata pointer in slot 0 instead of byte-copying into stack array; fixed in gen_local_decl with a per-byte-store path for Type::Array(Char,N)+StringLit, zero-padding to N. 4 new structural codegen tests. demo63 simplified — removed NOTE workarounds, now exercises both fixed paths at runtime (global char* with concat + escape, local char[] with concat, char[10] zero-pad). Two stale .word-counting tests updated to assert current .zero N form.