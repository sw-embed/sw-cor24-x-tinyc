Re-applied the previously-blocked emit-zero-fill change. Replaced per-word loop in emit_zero_fill (data.rs) with single .zero N emit. cor24-asm round-trip verified byte-identical .lgo (chunk_storage[64KB]: .s shrinks 21,900 lines -> 29; .lgo unchanged). 6 codegen tests pass; 60/64 demos pass (same 4 pre-existing failures); chibicc-subset 5/5; clippy clean. Demo runners now use cor24-emu --lgo (post-migrate-toolchain) so the cor24-run internal-assembler bug that blocked the first attempt is gone.