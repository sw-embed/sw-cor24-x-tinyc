Investigated 3 demos (2, 8, 10) flagged with exit_code=1. Root cause turned out to be a runner-script bug (not transpiler): grep 'LED:' fails to match cor24-emu's 'LED D2:' format; with set -euo pipefail, that exit-1 kills the script before validation. The transpiler was producing correct output for all three. Fixed all three in one shot since they share a single-line fix; also added awk filter to drop the random tempfile-path line so reg-rs goldens are stable. Updated 3 .rgt files to exit_code=0 and rebased baselines. 64/64 demos now pass.