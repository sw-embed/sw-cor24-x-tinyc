Fix: implicit string literal concatenation ("str1" "str2" → "str1str2") is not supported. This is standard C89 behavior. When the lexer/parser finishes scanning a string literal, check if the next token is also a string literal and concatenate them. This is needed by asm multi-string, macro tests, and general C compatibility. Test with adjacent string literals in expressions and initializers.