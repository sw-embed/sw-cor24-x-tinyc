Cast expression parsing fully implemented and verified. Expr::Cast AST node exists, parser detects (type)expr via is_type_start lookahead, codegen passes through, type inference returns cast type. Supports (int)x, (char *)p, (void *)0, nested casts, unsigned/signed casts, statement expressions with casts. All 5 subset tests pass, 6/41 chibicc tests pass (cast-dependent tests blocked by other features: float literals, ++*deref, complex sizeof types - not cast parsing).